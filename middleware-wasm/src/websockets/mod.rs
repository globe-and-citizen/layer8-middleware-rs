/// Websockets module
///
/// This module provides a websocket client to be used by the server.
/// The module is only available when the `websockets` feature is enabled and it is experimental at the time of writing.
pub mod websockets;

// #[cfg(feature = "websockets")]
